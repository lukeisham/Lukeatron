# ProjectKanban — Refactor Registry

kind: app
aliases: Dashboard app, Dash Board app, Project Dashboard (colloquial — the actual retired app of that name is archived separately, see `Archive/ProjectDashboard-app-2026-09-12/`)
template: `_template/`
test: none — Luke declined a `_test/` copy, 2026-09-12
last_updated: 2026-09-15 (drag-and-rearrange Next Actions + next-action lane colour built — see Migration log)

## In two lines
A Kanban board over every tracked Lukeatron project, opened in a browser from Dropbox. Replaced
ProjectDashboard as the project dashboard kept live on `:8789`.

## Next step
**The long-running `:8789` server process needs a manual restart** to pick up this session's
Python-side changes (`server.py`'s new `reorder` field, `model.py`/`stores.py`'s `multi_stream`,
`writes.py`'s `reorder_next_actions`) — static files (JS/CSS) serve fresh on every request and
already reflect the changes, but Python is loaded once at process start. Kill whatever holds
`:8789` (`lsof -ti:8789 | xargs kill`, or find and quit the terminal running it) and either
re-run `Start ProjectKanban.command` or let the next session's SessionStart hook
(`ensure-kanban.sh`) relaunch it. This agent's own tools could not do this — the shell they run
in cannot see or reach the process actually bound to `:8789` (confirmed: `lsof`/`ps`/a raw socket
connect all fail to find it from that shell, even unsandboxed, while the Browser pane reaches it
fine) — a real restart needs a hand with host-level terminal access. Once restarted, drag/move
in a project's Next Actions list should work end-to-end; this was otherwise fully verified via
the automated test suite (`writes.py`'s reorder primitive has real byte-level on-disk assertions,
independently reviewed) and via direct browser inspection of the parts servable without a
restart (lane colours, `multi_stream` gating). No other change pending.

## Where things are
```
ProjectKanban/
├── _template/              the canonical copy — raw working code + README.md, no data, no specs
├── refactor-registry.md    this file
└── wishlist.md             the project's idea backlog, carried forward from Sandbox, still live
```
No `_test/` — Luke declined one; the refactor loop below (STEP 7 of `!AppRefactor`) does not apply
until a test copy exists. Changes, if any arise, would need `_test/` built first.

## Granted rule exceptions
Carried forward from the build. Must match `_template/README.md` exactly.

| Rule ID | Where | Reason | Granted on |
|---|---|---|---|
| SR-4 — share, don't copy-paste | The whole app: its own copies of the parsing, write and palette modules rather than sharing ProjectDashboard's | A new app expected to replace the old one; divergence was the intended outcome | 2026-09-10 |

## Resume block
A cold agent picks this up by reading, in order:
1. This registry — nothing is currently open.
2. `_template/README.md` — key decisions and their reasons, cross-boundary behaviour, navigation map.
3. `wishlist.md` — the project's live idea backlog.
Do not reconstruct state from conversation history. This file is the state.

## Migration log
| Date | Event |
|---|---|
| 2026-09-12 | Moved from `System/Sandbox/ProjectKanban/build/` to permanent home `System/Apps/ProjectKanban/_template/`. Copy verified: 57 files, file-by-file diff clean, executable bits preserved. 140 Python unittest + 91 node test + 23 contrast checks all passed, both pre- and post-copy. |
| 2026-09-12 | Promoted to the live project dashboard on `:8789`, replacing ProjectDashboard: `server.py` PORT changed 8790→8789; new `ensure-kanban.sh` wired into `.Claude/settings.json`'s SessionStart hook; `.Claude/launch.json`'s `project-dashboard`/`project-dashboard-TEST` entries replaced with `project-kanban`; old ProjectDashboard app archived to `Archive/ProjectDashboard-app-2026-09-12/`; `CLAUDE.md` and `!HouseStyle/reference/sources.md` updated to point here. |
| 2026-09-12 | Luke resolved the three outstanding decisions: no `_test/` copy; lane colours collapsed to the house two-accent cap (`app/tokens.css`'s five `--l-*` tokens now all resolve to `--ink-muted`; `board.css`'s `data-lane-hues="mono"` toggle is a no-op, kept wired); Sandbox design documents deleted (`ProjectKanban-prd.md`, `_specs/`, `build.md`, `registry.md` — `System/Sandbox/ProjectKanban/build/` and `wishlist.md` were left in place, not part of the confirmed deletion scope). |
| 2026-09-12 | Wishlist #9 and #10 built directly in `_template/` — no `_test/` exists, so this bypassed the normal Phase 4 refactor-board/Health-Check loop (STEP 7 of `!AppRefactor`), same as the lane-colour collapse above. Verified against the code, not assumed: #9 — `card.js` `buildCopyIcon()` (~line 184) replaces the "Copy" text button with a two-overlapping-rectangles SVG, `aria-label` unchanged; #10 — `card.js` `buildGlyphIcon()` (~line 140) wraps each glyph in a `board-glyph-tip` span so `card.css`'s `::after` renders an instant styled tooltip from the existing `title` attribute, rather than the browser's native slow one. Both rows marked `built` in `wishlist.md`. |
| 2026-09-12 | Wishlist #11 (twelve glyph toggles → disclosure menu) closed out. The code was already built — `app/controls/glyph-menu.css` (new file), the "Glyphs" toolbar group in `index.html` restructured to one `glyph-menu-toggle` trigger plus a `popover` panel, `countActiveGlyphs`/`glyphMode` added to `glyph-switches.js`, `renderGlyphMenu` added to `controls.js` — but plan `glyph-toggle-menu-refactor.md`'s own closing steps (wishlist status, this registry row) had been left undone. Re-verified rather than assumed: `test_controls.mjs` 27/27 and `test_board.mjs` 21/21 pass; live check against the running `:8789` server confirmed one "Glyphs: all ▾" trigger at rest, opening to exactly "Display all" / "Display none" / "Display individual glyphs". Row marked `built` in `wishlist.md`; plan moved to `System/Plans/Completed/`. |
| 2026-09-12 | Wishlist #4b (show/hide done actions) and #5 (auto-refresh) built together per `projectkanban-autorefresh-show-done.md`. #4b: `model.py`'s `ProjectView` gained a `done_tasks` list built alongside (never replacing) `tasks` — `test_model.py` proves every open-side field (`tasks`/`open_count`/`lane`/`due_*`/`glyphs`) is byte-identical with or without done rows present; `app/project/actions.js`/`task-row.js`/`project.js` gained a "Show done actions (N)" toggle, off by default, revealing read-only dimmed/struck-through rows. #5: new stat-only `stores.latest_registry_mtime()` (walks `_tracking.yaml`'s resolved project paths, stats `registry.md`/`notes.md`, never re-parses content) backs a new `GET /api/board-changed.json`; new `app/controls/auto-refresh.js` polls it every 30s via `createPoller`, reloading (the same `location.reload()` the manual Refresh button already uses) on a real change unless focus is in a form control, in which case the change is retried next tick. Test coverage added: `test_model.py` (+3), `test_project.mjs` (+5), `test_stores.py` (+5), `test_server.py` (+3), and a new `tests/test_auto-refresh.mjs` (13 tests, dependency-injected poller — no real timer/fetch/reload needed). Verified live against `:8789`: `done_tasks` populates from real project data (CH-01, 12 done rows) in both light and dark palettes; a real registry file's mtime bump triggers a reload end-to-end, is deferred while a due-date input is focused, and fires on the next tick once focus moves on. Two pre-existing, unrelated test failures surfaced running the full suite (`project.css`'s box-shadow colour literal; a stale `:root[data-palette=...]` fixture in `test_check_contrast.py`) — both already logged in `Logs/issues.log`, neither touched by this change. Both rows marked `built` in `wishlist.md`. |
| 2026-09-14 | Dormant MinorTasks queue reader removed, following the queue's retirement the same day (`Trash/MinorTasks-queue-retired-2026-09-14/`). Direct edit in `_template/` — no `_test/` exists, same bypass as the 2026-09-12 rows. `stores.py`: module docstring, `QueueRow`/`_QUEUE_FIELDS`/`_QUEUE_ALIASES`/`read_queue`, and `BoardSources.queue`/`queue_skipped` (plus the `load_board_sources` call) removed; two comments no longer mention the queue. `model.py`: scope note and `_is_open` docstring drop the queue. `README.md`: `queue.md` removed from the `stores` sources row. `app/controls/controls.css`: comment only, no rendered change. `tests/test_stores.py`: `TestQueue` (2 tests) and `read_queue` in the public-API check removed; fixture `tests/fixtures/queue_sample.md` moved to `Trash/MinorTasks-queue-retired-2026-09-14/ProjectKanban-fixture/`. Nothing read `BoardSources.queue` (server, model, app), so behaviour is unchanged. Verified: Python 139 run (141 − 2 removed), node 92/93, `load_board_sources` against the real tree returns 45 projects / 0 skipped / 2 plans. The same two pre-existing failures as before the change (`test_check_contrast` palette fixture; `project.css` colour literal), both already in `Logs/issues.log`, untouched. |
| 2026-09-14 | Wishlist #19 (hide/reveal Decision Log) built — this was also LU-02-projectkanban's own Next Action #1. Direct edit in `_template/`, no `_test/`, same bypass as every row above. `app/project/sections.js`'s `buildDecisionLogSection` gained a `showDecisionLog`/`onToggleDecisionLog` pair and a `buildDecisionLogToggle` helper, reusing wishlist #4b's done-toggle pattern exactly (same `project-copy-btn` class, same `aria-expanded`/count-in-label shape); `app/project/project.js`'s `state` gained `showDecisionLog` (default `false`), a `toggleShowDecisionLog` handler, and the same fresh-navigation reset `showDone` already had. No new CSS. Verified live on `:8789` against LU-02-projectkanban's real 4-entry Decision Log: closed by default, expands to all 4 entries in file order on click, collapses back. `test_project.mjs` gained 4 cases (49 total, 48 pass — the 1 failure is the pre-existing `project.css` colour-literal issue, confirmed unrelated by running the unmodified suite on the same failure). Row marked `built` in `wishlist.md`. |
| 2026-09-14 | Wishlist #20 (Documents/People row names in the house accent colour) built — raised live in conversation, not pre-filed. Direct edit in `_template/`, no `_test/`, same bypass as every row above. `app/project/sections.js`'s `buildDocumentsSection`/`buildPeopleSection` added `project-row-text--accent` alongside the existing `project-row-text` class on the basename/name+role span (Events left untouched, per Luke's explicit scope); `app/project/project.css` gained `.project-row-text--accent { color: var(--accent); }` — the app's one house accent hue, already used for `border-color`, now used as text for the first time. `check_contrast.py` gained an `--accent`-on-`--panel-bg` row in both `PAIRS` and `PRINT_PAIRS`, since AC-4 requires every text/ground pairing actually used in the CSS to be covered, not just the ones that predate this change; measured 4.55:1 (print, the tightest) to 6.68:1 (dark) — all above the 4.5:1 floor, confirmed by running `check_contrast.py` itself. Verified live on `:8789` against CH-08-manse-building's real Documents (4 files) and People (6 rows), in both default and dark palettes — Events rows confirmed still plain ink via computed `color`. `test_project.mjs` gained 1 case (50 total, 49 pass — the 1 failure is the same pre-existing `project.css` colour-literal issue as every row above, unrelated). Row marked `built` in `wishlist.md`. |
| 2026-09-15 | `wishlist.md` cleaned up per Luke's request: every `built` row (#4b, #5, #9, #10, #11, #12, #19, #20 — all already logged above or in earlier rows of this table) deleted from the Ideas table, since their build history lives here, not there; #13 (state/lane heading position) also deleted, having been built outside this log (no row added here — Luke reported it done, not this session's own work, so there is no build detail to record). Both rejected rows also deleted from the wishlist per Luke's follow-up request, their reasoning preserved here instead: **#7 — implement linked-action sync** (rather than refusing to edit a linked row) was rejected 2026-09-12; the PRD fails closed by design (a linked row stays read-only) — doing it properly means writing `_links.yaml` and every sibling registry in one transaction, which is cross-registry write logic `!ProjectSweep` already owns, so a second, competing implementation here would duplicate an owner rather than fill a gap. Read-only-on-linked-rows stands as permanent behaviour unless Luke asks to revisit it. **#8 — capacity, snapshot, and plain-text brief** (the other three retired ProjectDashboard capabilities once queued for porting) was rejected 2026-09-10; archived, not ported. The fourth capability, unblock-next, was built directly into `!ProjectSweep` (`.Claude/skills/!ProjectSweep/skill.md` v1.4.0, 2026-09-12) rather than into this app. Historical implementation of all four remains in `Archive/ProjectDashboard-app-2026-09-12/`. `wishlist.md`'s own "Status values" line updated to match `!AppWishlist`'s canonical rule: only `open`/`adopted` persist in the table; `built` and `rejected` are deleted on the spot, not kept as dead statuses — the file had drifted from that rule (built/rejected rows had been accumulating with detail instead of being removed), this row is the correction. |
| 2026-09-15 | UI cue for which Next Action row drives the project's board lane — raised live in conversation (Luke asked whether it's clear anywhere that lane placement follows the highest-demand row, not the first-listed one; it wasn't). Direct edit in `_template/`, no `_test/`, same bypass as every row above. `model.py`: `TaskView` gained `lane_source: bool = False`; new `_lane_driver(tasks)` names the single open task `_project_lane` actually rolls up from (`min` by `_LANE_DEMAND_RANK`, first-of-ties), and `_project_lane` now calls it so the two can never disagree; `build_project` marks that one task `lane_source=True` via `dataclasses.replace`, skipped entirely under the `incoming` wake override since no task drives that answer. `app/project/task-row.js`: a bare `⚑` glyph renders next to the lane control on whichever row carries `lane_source`, in both `buildTaskRow` and `buildLinkedRow` — first shipped with a visible "sets board lane" label, then reduced to the glyph alone on Luke's follow-up ask the same conversation, `title` and `aria-label` carrying the full explanation on hover/for screen readers instead. `app/project/task-row.css`: `.project-task-lane-source` — muted text, no new colour, so it survives the mono lane-hue toggle. Verified live on `:8789` against PP-06-whatsapp-mcp (2 open rows): row 1 (Unshaped) unmarked, row 2 (Mine) marked with the bare glyph, matching `/api/board.json`'s `lane: mine` and confirming the badge lands on the actual driver, not the first row; `outerHTML` read back from the live page to confirm `title`/`aria-label` carry the text. `test_model.py` gained 3 cases (36 total, pass); `test_project.mjs` gained 2 cases, one asserting the glyph-only text plus both attributes (52 total, pass). Two pre-existing, unrelated failures surfaced running the full Python suite under `unittest discover` (`test_check_contrast`'s `project.css` colour-literal issue; a `test_writes.py` cross-test-isolation flake) — both absent when their own files run in isolation, neither touched by this change. |
| 2026-09-15 | Board card's next-action line coloured by its own resolved lane — Luke's request, planned as `System/Plans/Completed/projectkanban-next-action-lane-colour.md`. `card.js`'s `buildCard` sets `dataset.lane` on `.board-card-next-action` from `project.next_action?.lane?.value` (`""` when there's no open next action, never the string `"undefined"`). Originally built (via a Haiku sub-agent) as TEXT colour using the five `--l-*` tokens directly — `check_contrast.py` caught that three of the five (`--l-delegate`/`--l-waiting`/`--l-unshaped`) fail 4.5:1 as text on `--panel-bg` in the default/paper/print palettes (as low as 2.66:1) — Luke chose a decorative border over darkening tokens or shipping dark-palette-only. `card.css` now paints a `--board-card-rail-width` left border in the lane's own colour instead of the text itself, which stays plain ink; `check_contrast.py`'s `PAIRS`/`PRINT_PAIRS` had the now-inapplicable text-contrast rows removed (a border colour carries no minimum-contrast rule) — a coordinator fix caught two bugs a sub-agent's own report missed: the print-palette duplicate rows were left behind on the first pass, and the border's own CSS selector initially lost to a higher-specificity `:not([data-lane=""])` guard and rendered fully transparent regardless of lane (fixed by moving the transparent default onto the bare `.board-card-next-action` class selector, letting the lane-specific attribute selectors — higher specificity — win). Verified live on `:8789`: `--l-mine`/`waiting`/`unshaped`/`delegate`/`incoming` each resolve to their own literal hex via `getComputedStyle`, in both default and dark palettes, and correctly collapse to `--ink-muted` under `data-lane-hues="mono"`. `check_contrast.py` now reports 24/24 pairs passing across every palette and print. Test coverage: two new cases in the board-card test file (dataset reflects a stated lane; empty/absent when there's no next action). |
| 2026-09-15 | Drag-and-rearrange for a project's Next Actions list, persisted to `registry.md` — Luke's request, planned as `System/Plans/Completed/projectkanban-next-actions-drag-reorder.md`. **Deliberately reverses `writes.py`'s own prior architecture decision** — its "Public API" section header used to read "named fields only, no positional API, which is also why there is no drag" (`writes.spec.md`'s own now-retired AD-2); Luke gave explicit go-ahead to persist reordering to disk (rather than a client-side-only convenience) when this plan was scoped, and that header comment is updated to describe the new capability instead of contradicting it. Scoped to single-table-block ("single-stream") projects only — a project split across multiple Next Actions table blocks (rare; the `registry_multi_stream.md` fixture shape) shows no drag affordance at all, since `model.py`'s flat `tasks` list carries no per-stream membership and building that is separate, larger work. New: `stores.py`'s `RegistryRecord`/`read_registry()` gained `multi_stream: bool` (counts Next Actions table blocks via the existing `_find_section`/`_find_table_blocks`), threaded through `model.py`'s `RegistryRecordLike`/`ProjectView`/`build_project` with no derivation. `writes.py` gained `reorder_next_actions(root, *, project_id, row_order, mtime)`, `ReorderMismatchError`, and a locator that finds the ONE table block whose row-id set exactly matches `row_order` (refusing, writing nothing, on zero or multiple matches) — reuses the exact fence/mtime/re-parse-verify/record guard sequence `set_cell` already has; every row's own cells are carried through byte-identical, only line position changes. `server.py`'s `POST /api/edit` gained a `"reorder"` field (validated: a list of ≥2 unique non-bool str/int ids) routing to it, plus a new `reorder_mismatch` (422) error code; `edits.js` gained its sentence. Client: new `app/project/reorder.js` (native HTML5 drag-and-drop, event-delegated on the list container — no library) plus keyboard-operable "Move up"/"Move down" buttons on every row (drag alone has no keyboard equivalent); non-optimistic by design, matching every existing per-row edit control's own pattern exactly — a drop/move disables the list, submits, and relies on the existing silent-refetch-and-rerender path rather than hand-splicing DOM state that would need reconciling with `grouping.js`'s stem-grouping recompute. `task-row.js`/`actions.js`/`project.js`/`task-row.css` wired through; a multi-stream project renders one explanatory note instead of any drag UI. Test coverage: `test_stores.py`/`test_model.py` (`multi_stream`, both fixture directions), `test_writes.py` (11 new cases — happy path with byte-level cell verification, mismatch/stale-mtime/fence refusals all confirmed to leave the file completely untouched), `test_server.py` (validation + happy-path + mismatch-mapping), a new `tests/test_reorder.mjs` (6 cases). Full suite: 174 Python tests / 1 pre-existing unrelated failure (a path-only `test_check_contrast` import quirk, not a real check failure); 95 Node tests, all passing. Independently reviewed (a second Haiku sub-agent) line-by-line for the write-path safety properties given this mutates real project files — approved. **Not yet exercised end-to-end against a real project**: the long-running `:8789` server needs a restart to load the Python-side changes (see this file's own "Next step", above) — the coordinating session's tools could not reach the process actually bound to that port to restart it themselves. `multi_stream` gating and the drag/move UI's presence were confirmed live regardless (those parts don't need a server restart — `multi_stream` was already reflected in `/api/board.json` from an earlier restart during this session, and the client UI is static files served fresh). |
| 2026-09-20 | Wishlist #3 (a quiet mark for a project gone stale — no `updated:` movement in N weeks) dropped at Luke's request; row deleted from `wishlist.md` per `!AppWishlist`'s rule. No reason was given, so none is recorded. |
