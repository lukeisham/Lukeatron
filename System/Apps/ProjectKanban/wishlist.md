# ProjectKanban — Wishlist

A running backlog of future ideas for this app/widget. Ranked Low → High by perceived complexity
so the cheapest wins surface first. Reworded lightly for clarity on the way in — intent kept,
nothing invented.

## Ideas

| # | Idea | Complexity | Raised | Status | Notes |
|---|---|---|---|---|---|
| 1 | Undo the last write — restore the cell's previous value from `Logs/edits.log` | Low | 2026-09-10 | open | Writes are single-cell and already logged, so the previous value is on disk. Complication: undo must re-take the mtime, so it can be refused like any other write. |
| 2 | Keyboard navigation — arrow keys between cards, Enter to open, `c` to copy | Low | 2026-09-10 | open | It is a daily-use personal tool; the mouse round-trip is the cost. |
| 3 | A quiet mark for a project that has gone stale — no `updated:` movement in N weeks | Low | 2026-09-10 | open | `updated:` is populated on every project. Passes the no-duplicate-marks test: staleness is carried by no lane, column or glyph. |
| 4a | A filter box — type and the board narrows live (project title, action text, person, glyph) | Low | 2026-09-10 | open | Offered for the PRD at Q10 and not taken. With 40 projects and 283 rows this is the likeliest thing to be wanted on day two. |
| 4b | Show/hide done actions — reveal the 68 completed rows the board currently ignores | Low | 2026-09-10 | built | **Built 2026-09-12.** `model.build_project` now builds a `done_tasks` list alongside (never replacing) the open `tasks` list — verified byte-identical `tasks`/`open_count`/`lane`/`due_*`/`glyphs` with or without done rows present (`test_model.py`). The per-project detail view gained a "Show done actions (N)" toggle (`actions.js`/`project.js`) revealing them read-only, dimmed and struck-through (`task-row.js`'s `buildDoneTaskRow`, `task-row.css`), off by default and reset on each fresh project visit. Verified live on `:8789` against CH-01 (12 done rows) in both light and dark palettes. |
| 9 | Replace the "Copy" text button with the standard copy icon | Low | 2026-09-12 | built | `.board-card-copy` in `card.js`/`card.css`. The accessible name is already separate from the visible label (`aria-label="Copy <title>"`), so only the glyph changes — can reuse the mark/cutout SVG convention just built for the twelve board glyphs, or a simpler two-overlapping-rectangles shape. |
| 10 | Hover-over text for glyphs | Low | 2026-09-12 | built | Partly already there: `buildGlyphIcon` in `card.js` already sets a native `title` attribute ("Person", "Coin — guessed", etc.) — but that's the browser's own slow, unstyled tooltip. Check with Luke on pickup whether that already satisfies this, or whether a faster/styled custom tooltip is what's actually wanted. |
| 12 | Figure out where/how a project's "Scraps & ideas" / "Constraints" / "Reference" sections are stored, and how the project skills read them back | Low | 2026-09-12 | built | **Traced 2026-09-12, confirmed with Luke 2026-09-12.** Full finding in `Memory/Long-Term/Lukeatron/memory-structure.md` ("`notes.md`'s four sections — write-only today"). The write path is confirmed built and working correctly: the board's "Add a note" section maps its four choices exactly onto `notes.md`'s four headings via `note-box.js` → `writes.py`'s `_NOTE_SECTIONS`. The other half is NOT built: nothing reads these sections back — not the app (`stores.py`/`model.py` never parse `notes.md`) and no skill (`!ProjectSweep` included). If Luke wants "Agent guidance" notes to actually steer future agent behaviour automatically, that's separate, unbuilt work — not filed as its own wishlist row yet. |
| 13 | Move the state/lane heading to the top of its column cell — currently sits vertically centred | Low | 2026-09-13 | open | Lane headers are owned by `board/board.js` + `board/board.css`. |
| 4 | Filter the board to one person — every project and action involving them | Medium | 2026-09-10 | open | Needs the person glyph's resolution working first (`people:`, the People section, and the Owner column). |
| 5 | Auto-refresh when a registry changes on disk | Medium | 2026-09-10 | built | **Built 2026-09-12.** A new stat-only `stores.latest_registry_mtime()` (never re-parses registry content) backs a new `GET /api/board-changed.json`; a new `app/controls/auto-refresh.js` polls it every 30s and calls the same `location.reload()` the manual Refresh button already uses when it sees a real change, deferring one tick while focus is in a form control. Verified end-to-end against the live `:8789` server and a real registry file: reload fires on a genuine change, is deferred while a due-date input is focused, and fires on the very next tick once focus moves on. The underlying "board.js has no redraw hook, so this reloads instead of patching in place" gap is real and separately logged (`Logs/issues.log`, 2026-09-12), not solved here. |
| 14 | General visual pass on buttons and the toolbar — current controls look rough/unpolished | Medium | 2026-09-13 | open | Raised alongside #13; no specific control named yet — a `!HouseStyle` pass over `controls/controls.css` and the card/board button styles. |
| 11 | Refactor the twelve glyph toggle buttons into a selectable menu | Medium | 2026-09-12 | built | **Verified 2026-09-12** — code was already built (`glyph-menu.css`, the `glyph-menu-toggle`/`glyph-mode` markup in `index.html`, `countActiveGlyphs`/`glyphMode` in `glyph-switches.js`, `renderGlyphMenu` in `controls.js`) and all 27 `test_controls.mjs` assertions plus `test_board.mjs` pass, but the plan (`System/Plans/New/glyph-toggle-menu-refactor.md`) and this row were never closed out. Confirmed live in the running app on `:8789`: one "Glyphs: all ▾" trigger at rest; opening it shows exactly "Display all" / "Display none" / "Display individual glyphs". Plan closed out and moved to `Completed/` same day. |
| 7 | Implement linked-action sync rather than refusing to edit linked rows | High | 2026-09-10 | rejected | **Rejected 2026-09-12.** PRD fails closed instead (a linked row is read-only) — deliberately, not as a stopgap. Doing it properly means writing `_links.yaml` and every sibling registry in one transaction; that's real cross-registry write logic that `!ProjectSweep` already owns, so building a second, competing implementation here would duplicate an owner rather than fill a gap. Read-only-on-linked-rows stands as the permanent behaviour unless Luke asks to revisit it. |
| 8 | Capacity, snapshot, and plain-text brief — the other three Dashboard capabilities once queued for porting | High | 2026-09-10 | rejected | Archived, not ported. The fourth capability, unblock-next, was built directly into `!ProjectSweep` (`.Claude/skills/!ProjectSweep/skill.md` v1.4.0, 2026-09-12) rather than into this app, so it never needed its own wishlist row here. Historical implementation of all four remains in `Archive/ProjectDashboard-app-2026-09-12/` for reference. |
| 15 | Merge the Kind (per-action lane, this app's own field) and State (per-action urgency, `!ProjectSweep`'s field) — they now share almost the same five-word vocabulary since the 2026-09-13 State rename (Mine/Delegate/Waiting/Incoming/Undefined vs `mine`/`delegate`/`waiting`/`incoming`/`unshaped`) | High | 2026-09-13 | open | Two columns doing conceptually the same "whose move" job. Simplest path: `!ProjectSweep` writes the same value into a row's Kind that it already computes for State, and `resolve_lane` keeps reading Kind as today — but `!ProjectSweep`'s whole-project State roll-up currently derives from State's own precedence order across Next Actions, so folding it into Kind means rewriting that roll-up, not a plain rename; touches every live registry's Next Action rows plus this app's own rollup function (`model._project_lane`). Needs a proper plan (`!CreatePlan`) before anyone touches it. |
| 16 | A project/task's State should be driven off of *which action is the Next Action*, not set independently of it | Medium | 2026-09-13 | open | Overlaps #15's Kind/State merge — both are pointing at the same underlying gap: today State is a value `!ProjectSweep` computes and writes, not a live derivation from "what's the Next Action right now." Worth folding into the same `!CreatePlan` as #15 rather than solving twice. |

<!-- 2026-09-13: a "new AI skill to triage Next Actions" idea (+ its calendar-events question) was logged here as #17/#18 and moved out same day at Luke's correction — it's skill-level system work, not a ProjectKanban app/UI feature. Now tracked in PR-01-lukeatron-future-improvements/registry.md's Future Agent Actions. -->

Status values: `open` · `adopted` (folded into the PRD or a spec — kept here for history) ·
`built` (ported into `_template/`, Phase 4 only) · `rejected` (considered and declined, not
silently dropped).

## Conflict log
<!-- Phases 1-3, and Phase 4 up to the PRD's retirement (Step 3) only. Stops updating once the
     PRD is gone — leave what's here as history, don't backfill after. -->
| Date | Wishlist item | PRD/spec section | Nature | Resolution |
|---|---|---|---|---|

Nature values: `duplicate` · `overlap` · `conflict`.
