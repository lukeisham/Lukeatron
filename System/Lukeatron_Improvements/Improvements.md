# Lukeatron Improvements — Live Plan Discussion

## Agent Instructions — read before touching the table

**Purpose.** This is a *live planning-discussion document* between Luke and the agent for new features, refactoring, and issue-fixing in the Lukeatron system. It is where ideas are talked through **before** they become work.

**How it works:**

1. **Luke adds a row** — a simple task name (col 2) and his initial comments (col 3). Col 1 (`#`) is a sequential row number, assigned retrospectively and auto-incremented for each new row (never reused, even once a row is logged and removed — see step 6).
2. **The agent responds** in col 4 with questions, concerns, alternatives, or agreement. This may go back and forth: either party may revise their comments in cols 3–4 until the row converges. Keep the discussion inside the cells (use `<br>` for turns, prefixing each turn `**Luke:**` / `**Agent:**` where helpful).
3. **No plan or project Next Action may be instigated from a row until both Luke and the agent are happy with it.** Agreement means Luke explicitly says the row is ready (or marks it so). The agent must never spin up a plan (`!CreatePlan`) or a Next Action from a row still under discussion.
4. **When a row is agreed:** create the plan (Major → `System/Plans/New/`) or a Next Action in the best-fit project per the normal CLAUDE.md routing gate, and put a link to it in col 5.
5. **If the comments in a row change after a plan/Next Action exists,** the linked plan or Next Action **must be updated** to match the revised discussion — the row is the source of intent; the plan follows it.
6. **When the linked plan/minor task is completed,** mark the row completed: set its Status to ✅ and strike through the task name (`~~Name~~`). Once marked ✅, append an entry to `Memory/Long-Term/Logs/completed_improvements.log` (format documented in that file's header, carrying over the row's `#`) and then remove the row from this table — the log is the permanent record, not this live discussion doc.

**Status legend (col 6):** 💬 discussing · 🤝 agreed (plan/task created, col 5 linked) · 🚧 in progress · ✅ completed · ❌ dropped.

**Housekeeping:** the agent should check this document when Luke references "the improvements doc" or proposes Lukeatron changes conversationally — new ideas belong here as rows, not straight into plans. All work arising still obeys the normal safety gates (`!Checkpoint`, Impact axis).

## Improvements Table

| # | Task | Luke's initial comments | Agent's questions / comments | Plan / minor task link | Status |
| :---: | :--- | :--- | :--- | :--- | :--- |
| 1 | Schedule !Initiative + cron heartbeat log | Make `!Initiative` run once daily using haiku-5.  | **Agent (2026-09-14):** ⚠️ Scheduling half superseded — `!Initiative` and its cron were retired with the MinorTasks queue; the cron heartbeat-log half still stands.<br>**Agent:** The one dormant piece of automation: `initiative-sweep.sh` exists but was never added to crontab — `!Initiative` only runs manually. Proposed: add the script's own suggested entry (`45 7 * * *`, 07:45 AEST daily). Companion fix: no `cron-*.log` files exist, so a cron dying silently would be invisible — have each cron wrapper append one timestamped line to `Logs/cron.log` per firing. Both Low-risk, ~one line each. Report: [System_Review_2026-07.md](System_Review_2026-07.md) §H1, §M5. <br>**Agent (pass 2):** Haiku — yes, easily: the script calls the `claude` CLI directly, so we add `--model claude-haiku-4-5-20251001` to the invocation (note: the current model is Haiku **4.5** — there is no Haiku 5 yet; confirm that's what you meant). Checked the script: it already has a lock file AND already appends to `Logs/cron-initiative-sweep.log`, so the heartbeat is built in for this one — the only true gap is the crontab line (`45 7 * * *`). I'd still add the same one-line heartbeat to the other three cron wrappers while we're in there. Scope then = 1 crontab line + 1 `--model` flag + 3 heartbeat lines. Say 🤝 and I'll queue it. | [Plan](../Plans/New/system-review-fix-pass-1.md) | 🤝 |
| 2 | Doc-vs-disk drift fix pass | Delete `GrammarParser.md`, make  `SermonSeriesPrep.md`, `CongregationalPrayer.md` 'planned', add three missing templates. What is the social media portal, let's get rid of it.| **Agent:** One documentation-alignment pass: (a) church.md / teaching.md / personal-productivity.md name 4 templates + 3 slash-commands that don't exist (`SermonSeriesPrep.md`, `CongregationalPrayer.md`, `GrammarParser.md`, `/sermonprep`, `/parse`, `/email`…) — rename to real assets or mark as planned; (b) CLAUDE.md template table: drop the 2 missing, add the 3 existing-but-unlisted (`Template_Contact.md`, `Template_TechSpec.md`, `Template_MLA_Reference.md`); (c) flip `status: New` → `Completed` on 4 plans already in Completed/; (d) descope or plan the social-media portal church.md implies; (e) two one-word fixes (System_guide.md path, "~33"→34 stores). High-impact (touches CLAUDE.md) so gated, but mechanical. Report §H2, §M4, §M6, §L1–L2. <br>**Agent (pass 2):** Decisions noted: delete `GrammarParser.md` references · mark `SermonSeriesPrep.md` + `CongregationalPrayer.md` as *(planned)* in church.md and CLAUDE.md · add the 3 existing-but-unlisted templates to the CLAUDE.md table. On the "social media portal": church.md lists external promo (Google ads, YouTube reels, Instagram/Facebook posts) as in-scope, but no send-capable portal exists — only email. "Get rid of it" = I'll edit church.md so promo content is *drafted and staged in `Outbox/` for you to post manually* (the current de-facto behaviour), and close the matching open row in `issues.log`. I'll also fold in the remaining mechanical bits: fix the 3 phantom slash-commands (`/sermonprep`→prose, `/parse`→delete with GrammarParser, `/email`→`!AgentMail`), flip the 4 plan `status:` fields, and the two one-word fixes. Say 🤝 for the full pass. | [Plan](../Plans/New/system-review-fix-pass-1.md) | 🤝 |
| 3 | Long-Term index doctrine: index.md vs empty _index.yaml | Which system is better `_index.yaml` or `index.md` - for integration into the Lukeatron Wiki and if necessary turning into temporary seperate wiki's to share with outsiders? | **Agent:** 24 of 34 Long-Term stores have 0-byte `_index.yaml`, while ALL 34 now have a populated `index.md` (OKF retrofit). CLAUDE.md still tells the agent to read `_index.yaml` — an agent following doctrine opens an empty file and may think the store is empty. Recommend option (a): update CLAUDE.md + memory-structure.md to name `index.md` as the navigation surface and retire the empty yamls, since that's where the system has actually moved. Alternative (b): populate the 24 yamls. Report §M3. <br>**Agent (pass 2):** For both of your goals, `index.md` wins clearly. **Wiki integration:** it's markdown — the wiki viewer and any renderer display it directly, and it already carries OKF frontmatter; `_index.yaml` is machine-catalog only and invisible to every renderer. **Temporary outsider wikis:** exporting a share-wiki becomes "copy the store's `.md` files, `index.md` is the landing page" — no conversion step; a yaml catalog would need transforming first. One carve-out: `LukeatronWiki/_index.yaml` is load-bearing (it's the idea-graph the agent traverses — nodes + `related:` edges), so it stays. Proposal: doctrine says `index.md` is the navigation surface; delete the 24 empty yamls; migrate anything useful from the other 9 populated yamls into their `index.md`; retain LukeatronWiki's graph yaml. Touches CLAUDE.md + memory-structure.md, so gated. Say 🤝 if that's the shape. | [Plan](../Plans/New/system-review-fix-pass-1.md) | 🤝 |
| 4 | Dormant-skill review + skills.log spec | Move `!GenerateWiki` to the skill bank. Some skills simply haven't been used yet because the system is so new. One open question: keep the skills.log one-page spec in scope? It's cheap (one doc, applied going forward, no retro-rewrite) and makes the next audit machine-checkable — I recommend keeping it = keep it. | **Agent:** 10 of 23 skills have never fired: core `!Calendar`, `!GenerateWiki`, `!MinorTask` (caveat: queue rows exist, so !MinorTask may fire without logging); Skillbank `!Dashboard`, plus `!BookCover`/`!TechSpec` (too new) and `!ThinPortal` (pattern, expected). Decide per skill: fix triggers, deliberately exercise once, or retire. Companion: skills.log has 6 format variants + 9 non-skill entries, making usage counts fuzzy — a one-page logging spec (canonical line format) applied going forward would make the next audit machine-checkable. Report §M1–M2. <br>**Agent (pass 2):** Agreed on both counts. Moving `!GenerateWiki` to the Skillbank is just the documented promotion path in reverse: move the folder to `System/Skillbank/GeneralPurposeSkills/!GenerateWiki/` (SKILL.md → skill.md), add its catalog row to `_index.yaml`, and update the CLAUDE.md skill table — High-impact (core-skill + CLAUDE.md), so gated, but small. It stays fully invocable; it just loads on trigger instead of at boot. And yes — the system is ~2.5 weeks old, so I'd leave `!Calendar`, `!MinorTask`, `!Dashboard`, `!BookCover`, `!TechSpec` untouched and re-check usage at the next review. One open question: keep the skills.log one-page spec in scope? It's cheap (one doc, applied going forward, no retro-rewrite) and makes the next audit machine-checkable — I recommend keeping it. Say 🤝 with or without the spec. | [Plan](../Plans/New/system-review-fix-pass-1.md) | 🤝 |
| 5 | Parser Foundations |I think its fine that these projects are still under construction. Eventually /Users/lukeishammacbookair/Library/CloudStorage/Dropbox/_Lukeatron/System/Suggestions/Parser_guide.md will be the foundation for each of the teaching projects. And files like /Users/lukeishammacbookair/Library/CloudStorage/Dropbox/_Lukeatron/System/Suggestions/Grammar_contents.md the contents of the grammar parser. <br><br>Grammar, Rethoric, Style, Fact-Checking, Greek, Hebrew, Logic, Interpretation, Story Tension, Tropes and Symbols, Biblical Theology and Systematic Theology = should all be seperate folders in Long term memory and medium term Parser projects. Greek, Hebrew, Biblical Theology and Systematic Theology will be Church parser projects and the rest teaching projects. ALl will share the same technical parser base. Once its ready it will form the basis of all the other apps. Each app will have different content and slightly different logic systems. | **Agent:** The largest purpose-alignment gap: all 5 TE projects are ⚪ undefined, and the context's named tooling (`GrammarParser.md`, `/parse`) doesn't exist — Teaching is the context where docs promise most and disk delivers least. Your call between: (1) shape TE-01 (grammar parser) first as the beachhead, (2) consolidate TE-01..05 into one "Teaching Apps" umbrella project, or (3) consciously park the context and note that in CLAUDE.md. Report §H3, §L4. <br>**Agent (pass 2):** Understood — intentional shaping phase, not a defect; the ⚪ states are correct as-is. Both files confirmed on disk (`System/Suggestions/Parser_guide.md`, `Grammar_contents.md`). One tiny Low-impact step so this intent lives somewhere durable instead of only in this table: record those two files as the declared foundation in the five TE project registries/notes (one line each). The teaching.md phantom references (`GrammarParser.md`, `/parse`) are already covered by the drift-fix row above. If you're happy with just the registry notes, I'll log it as a minor task and this row can close; or we can drop the row entirely and leave things as they are. <br>**Agent (pass 3):** This is now a real architecture, not a parking decision — worth renaming this row "Parser platform". What I've captured: **one shared technical parser base** built first, then ~12 domain apps on top (Grammar · Rhetoric · Style · Fact-Checking · Logic · Interpretation · Story Tension · Tropes and Symbols as Teaching parser projects; Greek · Hebrew · Biblical Theology · Systematic Theology as Church parser projects), each with its own content + slightly different logic; each domain gets a Long-Term store and a Medium-Term project. Notes: `Grammar`, `Rhetoric`, `Logic`, `Theology` stores already exist (does Theology split into Biblical/Systematic, or do both parser projects feed the one store?); the other stores would be new. Proposed durable capture now (Low-impact): write this architecture as a one-page note beside `Parser_guide.md` and reference it from the TE registries — then, when you say the parser base is ready to start, a shaping plan creates the store/project skeletons and the base project. Want the capture note now and the shaping plan later? | | 💬 |
| 6 | Lukeatron UI / UX style guide | I want to create a style guide for the Lukeatron UI/UX, including colour schemes, typography, and component guidelines. <br><br>a) Which of these sites is the easiest for you to access? https://m3.material.io and or https://atlassian.design/components or do you know of a similar site so that we are both talking the same style language and have a source inspiration. <br><br>Light and darkn mode, mobile section if I build a Mobile phone extension in the future. | **Agent:** Both are accessible to me (JS-heavy, but the browser portal reads them fine), so pick on fit, not access. My recommendation: **Atlassian Design System** as the primary reference — it's built for exactly what Lukeatron's tools are (information-dense admin surfaces: boards, tables, status lozenges, side-nav), so its component patterns map 1:1 onto the dashboard. Borrow **Material 3's token vocabulary** (colour roles like primary/surface/on-surface, the type scale) as the naming system underneath, since it's the cleaner shared language for "what do we call this colour." One scoping suggestion: the style guide should govern the **dashboard + future tools only** — the wiki viewer's stated goal is to look like *Wikipedia*, which is neither M3 nor Atlassian; give it a one-paragraph carve-out. Deliverable: `Memory/Long-Term/Style Guide/Lukeatron_UI_Style_Guide.md` (colour roles incl. the existing state colours 🟢🔵🟠🔴⚪, type scale, spacing, component inventory). Two questions: light-mode only or also dark? And desktop-only, or should it cover your phone too? <br>**Agent (pass 2):** Both answered — scope is now settled: Atlassian components + M3 token vocabulary, **light + dark modes**, a **mobile section** (guidelines only, held for a future phone build), Wikipedia carve-out for the wiki viewer, deliverable `Style Guide/Lukeatron_UI_Style_Guide.md`. Nothing left to discuss on my side — mark 🤝 and I'll create the plan (Major: web research on both design systems + a Long-Term store write). The two dashboard/wiki rows below should queue behind it. | | 💬 |
| 7 | Project Dashboard UI/UX improvements | a) using the style guide we created above, b) I want to improve the apperance and experience of the dashboard, the end goal is for me to clearly see the overall state of a project and then drill down into tasks for more detail, progressively seeing details with only a few clicks or hovers. c) I like the overall layout and colour scheme for states and the ability to add scraps of information or mark as done. This change is less about functionality and more about improving the user experience and apperance. <br><br>Behaviour: level two = hover, level three = click and expand. I feel as though the icons / glyphs could be made uniform and a bit more baroque or interesting | **Agent:** Clear brief — keep the layout, state colours, scraps and mark-done; polish the surface and sharpen progressive disclosure. Proposed shape (blocked on the style-guide row above, which should land first): **three levels** — (1) board: project cards showing only name, state colour, and a one-line "next thing" so overall health reads at a glance; (2) hover/tap: a preview card with the top 2–3 open items, waiting_on, and wake date — no click needed; (3) click: full project detail (registry items, scraps, done-marking) as now. Plus consistent typography/spacing/elevation from the style guide. All read-path changes to `project-dashboard/serve.py`; edit-mode behaviour untouched. Questions: is hover the right level-2 gesture (i.e. is this desktop-only), and is there anything on the current board you'd *remove* while we're in there? <br>**Agent (pass 2):** Locked in: level 2 = hover preview, level 3 = click-and-expand. On icons — agreed they're currently ad-hoc; "uniform and a bit more baroque" reads to me as: one consistent glyph family across the whole dashboard, with more ornamental character than plain outline icons — think engraved/woodcut-style SVG set or a decorative serif ligature set rather than Material's geometric minimalism. That choice belongs in the style guide (row above) as the icon section, then this row applies it. So: this row is well-defined but **blocked on the style guide** — I suggest marking it 🤝 only after that plan completes, so the plan here can cite actual tokens/glyphs rather than placeholders. | | 💬 |
| 8 | Lukeatron Wiki UI/UX improvements | Purpose = I want to keep making the wiki look more like Wikipedia and remove a few unnecessary bits. a) I want to remove the "In this topic" section from the wiki. b) I want to visual way to see content and connections between wiki pages. c) I want to improve the Backlog section to make it more inviting for me to use. <br><br>What is uninviting is the wall of undifferentiated links and no sense of what's next, maybe thumbnails would help, plus a reorginsation function based on how i'm interacting with the rest of the wiki? | **Agent:** All three are viewer-side (`lukeatronwiki-viewer/serve.py`), read-only preserved. (a) Removing "In this topic" — trivial, one render function. (b) Visual connections — the data already exists (`related:` edges in `LukeatronWiki/_index.yaml` + the backlinks the viewer computes), so I'd add a `/graph` page: nodes as pages, edges as relations, click-through to the page — no new data entry needed. Caveat worth fixing first via `!IdeaWiki`: the graph currently has 34 one-way edges and 2 dead links (from `issues.log`), which a visual view will make very visible. Wikipedia-look equivalent would be sidebar "connection maps" per page rather than one global hairball — I'd do per-page local graphs (this page + 1 hop) plus a global view. (c) Backlog — before I redesign it, tell me what makes it uninviting now: is it a wall of undifferentiated links (needs grouping by store/type?), no sense of "what next" (needs a suggested-next-read?), or just visually flat (thumbnails/descriptions per entry)? <br>**Agent (pass 2):** Clear — so the Backlog redesign = **thumbnail cards** (the wiki already has a thumbnail convention + `Thumbnails/` store) grouped rather than one flat list, plus a **"suggested next"** strip at the top. On "reorganisation based on how I'm interacting with the rest of the wiki": the viewer currently has no memory of what you visit — it's deliberately read-only. Two ways to get interaction-aware ordering: (1) the viewer keeps a tiny local visit-count log (one JSON file, viewer-owned, not Memory/) and ranks the Backlog by what you've been reading around lately — a small, honest exception to "never writes" since it writes only its own telemetry, never your content; or (2) agent-side: rank by which wiki nodes/stores recent sessions have touched — no viewer writes, but coarser. I'd take (1) for accuracy. Which sits better with you? (The `/graph` view and "In this topic" removal from pass 1 stand as proposed; graph hygiene fix is queued via the Minor-fixes row below.) | | 💬 |
| 9 | Browser based content ingester | I want a browser-based content ingester that allows me to easily ingest plain or formatted text content into either Projects, minor queue or the Lukeatron wiki. (long term memory) <br><br>Maybe a visual suggested list so that I can say yes this goes there or here etc. With a too hard too ambigious dump in Inbox option to deal with later <br><br>I think feeding into the Inbox is a good idea. But a function where I can add (optoinal) helper info to the content before ingesting it would be even better. eg material for Wiki or Project task or personal contact details etc | **Agent:** Good fit with the existing architecture — this is a friendly front-end to `!Intake`, and your "dump in Inbox" fallback is exactly the system's fail-closed default. Proposed shape: an `/ingest` page (new tab on the dashboard, or a third tiny tool) — paste text → it suggests destinations using the four !Intake outcome-axes (this project · minor queue · wiki/store · just archive) → you click yes/no per suggestion → "too hard" button dumps it to `Inbox/` untouched. One design decision to settle up front: both viewers are deliberately **read-only**, and I'd protect that. Two options: (1) the ingester writes ONLY staging files into `Inbox/` (your click = a routing hint in the file's frontmatter) and the normal `!Intake` sweep executes the actual routing with all its gates — viewers stay read-only in spirit, safety model unchanged; (2) the ingester writes directly to queue/wiki/projects — snappier, but creates the system's first write-capable browser tool and bypasses `!Intake`. I strongly recommend (1); latency is at most one intake sweep (4×/day, or an on-demand "process now"). Which way do you want it? <br>**Agent (pass 2):** Inbox staging confirmed, and the helper-info idea slots in perfectly: the ingest form gets an optional **hints panel** — a destination hint (Wiki material / Project task / Minor task / Contact details / General), an optional free-text note ("this is for CH-05", "file under Rhetoric"), and both get written into the staging file's frontmatter (`hint:`, `note:`). `!Intake` then treats your hints as strong routing signals — it still runs its own four-axis assessment and all gates, but a hint usually decides the call, and hinted items should route with near-zero ambiguity. Unhinted or "too hard" items just land as plain Inbox drops, exactly as now. Design feels settled to me: `/ingest` page → staging file in `Inbox/` with optional hints → normal gated `!Intake` routing + an on-demand "process now" button that just asks the agent to run the sweep. Mark 🤝 when you're happy and I'll plan it. | | 💬 |
| 10 | Minor fixes | **L1.** `System_guide.md` line 175 says skills graduate to `System/.claude/skills/` — correct path is `.claude/skills/`. One-word fix. <br>**L2.** CLAUDE.md says "~33 stores" — there are 34. Cosmetic. <br>**L3.** LukeatronWiki graph hygiene (already open in `issues.log`): 2 dead `[[theology-aphorisms]]` links in `meta-about.md`, 34 one-way `related:` edges, landing page absent from `_index.yaml`. Fix via `!IdeaWiki` in one tending pass. | **Agent:** Partial overlap with work already agreed: **L1 and L2 are both inside the fix-pass plan** (steps 3–4 of [system-review-fix-pass-1](../Plans/New/system-review-fix-pass-1.md)) — no separate row needed for them. **L3 is not covered anywhere yet** and is worth doing soon, since the wiki `/graph` view (row above) will make every one-way edge and dead link visibly ugly. It's a self-contained one-off: a single `!IdeaWiki` tending pass (fix 2 dead links, mirror 34 edges, add the landing page to `_index.yaml`) — I'd log it as a minor task rather than a plan. Say 🤝 and I'll queue exactly that; the L1/L2 lines can then be struck from this row. | | 💬 |
| 11 | Cron execution leaves no log trail | **Evidence:** No `cron-*.log` files exist in `Logs/`; scheduled-slot evidence had to be inferred from `skills.log` entries. Whether a given run was cron-fired or manual is currently indistinguishable. <br>**Impact:** A cron silently dying (e.g., after an OS update revokes permissions) would be invisible until someone noticed missing digests. <br>**Proposed fix:** Have each cron wrapper script append one timestamped line to a `Logs/cron.log` on every firing (even on skill failure) — the cheapest possible heartbeat. | **Agent:** Already fully covered — this is inside the scope of the "Schedule !Initiative + cron heartbeat log" row you marked 🤝 above: step 1 of [system-review-fix-pass-1](../Plans/New/system-review-fix-pass-1.md) adds the heartbeat line to all four cron wrappers (with a syntax-check test, and a live check at the next 07:45 firing). Recommend marking this row ❌ as a duplicate — it will be delivered by that plan. | | ❌ |
| 12 |  | | | | 💬 |
