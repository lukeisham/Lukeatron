/* Psychometric cartridge EXPLAINER — present mode (GeneratorShell.spec.md
   §3b/FR-D3) does not use EXPLAINER's nine-key analyse-mode contract;
   explanation is ENGINE.explainItem's job instead. files.explainer stays a
   required manifest field, so this placeholder ships per the spec. */
var EXPLAINER = {};

if (typeof module !== "undefined" && module.exports) {
  module.exports = EXPLAINER;
}
